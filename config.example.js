// Copy this file to config.js and fill with your Firebase project's web config.
// You can find these values in Firebase Console → Project Settings → General → Your apps (Web app).
// IMPORTANT: Firestore must be enabled (in Test mode is fine for demo) and Authentication → Sign-in method → Anonymous enabled.
window.FIREBASE_CONFIG = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_AUTH_DOMAIN",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_STORAGE_BUCKET",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};
